---
title: "[프로그래머스 12909] 올바른 괄호"
date: 2026-04-30
last_modified_at: 2026-04-30
categories: [algorithm, stack]
tags: [programmers, java, stack]
toc: true
comments: true
---

## 문제

> 링크: [프로그래머스 12909](https://school.programmers.co.kr/learn/courses/30/lessons/12909)

괄호 문자열이 올바른지 판단한다. `(`와 `)`로만 이루어진 문자열이 주어졌을 때, 모든 괄호가 올바르게 짝지어지면 `true`, 아니면 `false`를 반환한다.

## 접근

### 카운터

스택 대신 정수 카운터 하나로 열린 괄호 수를 추적한다.

- `(` → 카운터 증가, `)` → 카운터 감소
- 순회 중 카운터가 음수가 되면 닫는 괄호가 먼저 나온 것이므로 즉시 `false`
- 순회 후 카운터가 0이 아니면 열린 괄호가 남아있으므로 `false`

O(n) 시간, O(1) 공간.

## 풀이

```java
package programmers.stack.p12909;

public class Solution {

    public static void main(String[] args) {
        Solution s = new Solution();

        String[] inputs = {"()()", "(())()", ")()(", "(()("};
        boolean[] answers = {true, true, false, false};

        for (int i = 0; i < inputs.length; i++) {
            System.out.println(s.solution(inputs[i]) == answers[i]);
        }
    }

    public boolean solution(String s) {
        int flag = 0;
        for (int i = 0; i < s.length(); i++) {
            if (s.charAt(i) == '(') flag++;
            else flag--;
            if (flag < 0) return false;
        }
        return flag == 0;
    }
}
```