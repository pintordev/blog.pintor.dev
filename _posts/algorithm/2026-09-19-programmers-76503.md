---
title: "[프로그래머스 76503] 모두 0으로 만들기"
date: 2026-09-19
last_modified_at: 2026-09-19
categories: [algorithm, tree]
tags: [programmers, java, tree]
toc: true
comments: true
---

## 문제

> 링크: [프로그래머스 76503](https://school.programmers.co.kr/learn/courses/30/lessons/76503)

트리의 각 정점에 가중치가 있을 때, 인접한 두 정점 사이에서 한쪽을 1 증가시키고 다른 쪽을 1 감소시키는 연산으로 모든 정점을 0으로 만들 수 있는지 판별하고, 가능하면 최소 연산 횟수를 구하는 문제.

## 접근

### 서브트리 합 기반 그리디

연산 한 번은 한 정점에서 +1, 다른 정점에서 -1이 일어나므로 전체 가중치 합은 항상 보존된다. 따라서 전체 합이 0이 아니면 애초에 모든 정점을 0으로 만들 수 없다.

트리에서 임의의 간선을 제거하면 두 서브트리로 나뉜다. 이 간선을 통해 흘러야 하는 이동량은 자식 쪽 서브트리 가중치 합의 절댓값과 같다. 부호는 이동 방향만 결정할 뿐, 필요한 연산 횟수는 그 절댓값으로 고정된다.

임의의 정점을 루트로 잡고 서브트리 합을 구한 뒤, 루트를 제외한 모든 정점에 대해 자신을 루트로 하는 서브트리 합의 절댓값을 더하면 그게 최소 연산 횟수가 된다.

정점 수가 최대 30만이라 재귀 DFS는 스택 오버플로 위험이 있어서, 명시적 스택으로 방문 순서를 기록한 뒤 역순으로 처리하는 반복문 기반 방식을 사용했다. 스택에서 pop한 노드를 방문 순서 배열에 기록하고 자식은 그 이후에 push하므로, 부모가 항상 자식보다 먼저 기록된다. 이 순서를 뒤집어 처리하면 자식의 서브트리 합이 부모에 누적된 뒤에 부모 자신이 처리되는 순서가 보장된다.

정점과 간선을 각각 한 번씩만 순회하므로 시간복잡도는 O(n), 공간복잡도도 O(n)이다.

## 풀이

```java
import java.util.*;

int n;
List<Integer>[] graph;
long[] a;
int[] parent;

public long solution(int[] a, int[][] edges) {
    n = a.length;
    this.a = new long[n];
    long sum = 0;
    for (int i = 0; i < n; i++) {
        this.a[i] = a[i];
        sum += a[i];
    }
    if (sum != 0) return -1;
    graph = new List[n];
    for (int i = 0; i < n; i++) {
        graph[i] = new ArrayList<>();
    }
    for (int[] edge : edges) {
        graph[edge[0]].add(edge[1]);
        graph[edge[1]].add(edge[0]);
    }
    return countOperations();
}

public long countOperations() {
    parent = new int[n];
    int[] order = new int[n];
    int idx = 0;
    Deque<Integer> stack = new ArrayDeque<>();
    stack.push(0);
    while (!stack.isEmpty()) {
        int node = stack.pop();
        order[idx++] = node;
        for (int next : graph[node]) {
            if (next == parent[node]) continue;
            parent[next] = node;
            stack.push(next);
        }
    }
    long cnt = 0;
    for (int i = n - 1; i >= 1; i--) {
        int node = order[i];
        a[parent[node]] += a[node];
        cnt += Math.abs(a[node]);
    }
    return cnt;
}
```
