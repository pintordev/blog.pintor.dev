---
title: "[프로그래머스 169198] 당구 연습"
date: 2026-07-20
last_modified_at: 2026-07-20
categories: [algorithm, math]
tags: [programmers, java, math]
toc: true
comments: true
---

## 문제

> 링크: [프로그래머스 169198](https://school.programmers.co.kr/learn/courses/30/lessons/169198)

당구대에서 공을 벽에 최소 한 번 반사시켜 목표 공에 맞힐 때, 굴러가는 최단 거리의 제곱을 구하는 문제다.

## 접근

### 벽 반사(거울 대칭) 트릭

시작점을 4개 벽(좌, 우, 상, 하) 각각에 대칭 이동시킨 가상의 점을 만들면, 그 가상의 점에서 목표 공까지의 직선 거리가 곧 해당 벽에 한 번 반사되어 도달하는 최단 경로 길이와 같다. 네 후보의 값 중 최솟값을 답으로 택한다.

단, 목표 공이 시작점과 x좌표 또는 y좌표가 같은 경우, 방향에 따라 벽에 닿기 전에 목표 공을 먼저 지나쳐버리는 퇴화 경로가 생길 수 있다. 이런 경우 해당 벽 후보는 제외해야 한다.

## 풀이

```java
public int[] solution(int m, int n, int startX, int startY, int[][] balls) {
    int[] res = new int[balls.length];
    for (int i = 0; i < balls.length; i++) {
        int a = balls[i][0], b = balls[i][1];
        int min = Integer.MAX_VALUE;
        if (b != startY || a >= startX) min = Math.min(min, sq(startX + a) + sq(startY - b));
        if (b != startY || a <= startX) min = Math.min(min, sq(2 * m - startX - a) + sq(startY - b));
        if (a != startX || b >= startY) min = Math.min(min, sq(startX - a) + sq(startY + b));
        if (a != startX || b <= startY) min = Math.min(min, sq(startX - a) + sq(2 * n - startY - b));
        res[i] = min;
    }
    return res;
}

public int sq(int x) {
    return x * x;
}
```
