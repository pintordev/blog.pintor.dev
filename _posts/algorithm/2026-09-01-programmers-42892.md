---
title: "[프로그래머스 42892] 길 찾기 게임"
date: 2026-09-01
last_modified_at: 2026-09-01
categories: [algorithm, tree]
tags: [programmers, java, tree]
toc: true
comments: true
---

## 문제

> 링크: [프로그래머스 42892](https://school.programmers.co.kr/learn/courses/30/lessons/42892)

각 노드의 `(x, y)` 좌표가 주어질 때, 규칙에 맞는 이진트리를 복원해 전위 순회와 후위 순회 결과를 노드 번호로 반환하는 문제.

## 접근

### 좌표 규칙 = 이진탐색트리

문제의 트리 규칙을 정리하면 다음과 같다.

- 왼쪽 서브트리의 모든 x < 노드의 x < 오른쪽 서브트리의 모든 x → **x 기준 이진탐색트리(BST)**
- 같은 레벨은 y가 같고, 자식의 y는 부모보다 작다 → **y가 큰 노드일수록 루트에 가깝다**

즉 노드를 **y 내림차순, y가 같으면 x 오름차순**으로 정렬하면, 그 순서가 곧 "위쪽 레벨부터, 왼쪽부터" 채우는 순서가 된다. 이 순서대로 BST에 삽입하면 원래 트리가 유일하게 복원된다.

삽입 시점에는 이미 들어간 모든 노드의 y가 현재 노드보다 크거나 같으므로, 루트에서 x만 비교하며 내려가다 빈 자식 자리를 만나면 거기에 붙이면 된다. 항상 올바른 리프 위치에 안착한다.

### 순회는 반복문으로

트리 깊이가 최대 1,000이라 재귀도 무리는 없지만, 명시적 스택(`Deque`)으로 반복 순회했다.

- **전위(root → left → right)**: 스택에서 꺼내 번호를 기록하고, 오른쪽 → 왼쪽 순으로 push 하면 왼쪽이 먼저 나온다.
- **후위(left → right → root)**: `root → right → left` 순으로 순회하면서 결과 배열을 **뒤에서부터** 채우면, 그 역순이 곧 후위 순회가 된다.

정렬 `O(n log n)`, 삽입 `O(n · 깊이)`, 순회 `O(n)`으로 제한(n ≤ 10,000, 깊이 ≤ 1,000)을 넉넉히 통과한다.

## 풀이

```java
import java.util.ArrayDeque;
import java.util.Arrays;
import java.util.Deque;

int n;
Node root;

public int[][] solution(int[][] nodeinfo) {
    n = nodeinfo.length;
    Node[] nodes = new Node[n];
    for (int i = 0; i < n; i++) nodes[i] = new Node(nodeinfo[i][0], nodeinfo[i][1], i + 1);
    Arrays.sort(nodes);
    root = nodes[0];
    for (int i = 1; i < n; i++) insert(nodes[i]);
    return new int[][]{preorder(), postorder()};
}

public void insert(Node node) {
    Node cur = root;
    while (true) {
        if (node.x < cur.x) {
            if (cur.left == null) {
                cur.left = node;
                return;
            }
            cur = cur.left;
        } else {
            if (cur.right == null) {
                cur.right = node;
                return;
            }
            cur = cur.right;
        }
    }
}

public int[] preorder() {
    int[] result = new int[n];
    int idx = 0;
    Deque<Node> stack = new ArrayDeque<>();
    stack.push(root);
    while (!stack.isEmpty()) {
        Node cur = stack.pop();
        result[idx++] = cur.num;
        if (cur.right != null) stack.push(cur.right);
        if (cur.left != null) stack.push(cur.left);
    }
    return result;
}

public int[] postorder() {
    int[] result = new int[n];
    int idx = n;
    Deque<Node> stack = new ArrayDeque<>();
    stack.push(root);
    while (!stack.isEmpty()) {
        Node cur = stack.pop();
        result[--idx] = cur.num;
        if (cur.left != null) stack.push(cur.left);
        if (cur.right != null) stack.push(cur.right);
    }
    return result;
}

class Node implements Comparable<Node> {
    int x;
    int y;
    int num;
    Node left;
    Node right;

    public Node(int x, int y, int num) {
        this.x = x;
        this.y = y;
        this.num = num;
    }

    @Override
    public int compareTo(Node o) {
        if (y != o.y) return o.y - y;
        return x - o.x;
    }
}
```
