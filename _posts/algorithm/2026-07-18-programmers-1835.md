---
title: "[프로그래머스 1835] 단체사진 찍기"
date: 2026-07-18
last_modified_at: 2026-07-18
categories: [algorithm, bruteforce]
tags: [programmers, java, bruteforce]
toc: true
comments: true
---

## 문제

> 링크: [프로그래머스 1835](https://school.programmers.co.kr/learn/courses/30/lessons/1835)

카카오프렌즈 8명을 일렬로 세울 때, 주어진 조건(간격 제약)을 모두 만족하는 배치의 경우의 수를 구하는 문제다.

## 접근

### 순열 완전탐색 (백트래킹)

인물이 8명으로 고정되어 있어 순열의 수는 8! = 40,320가지로 작다. 모든 순열을 백트래킹으로 생성하면서, 순열이 완성될 때마다 주어진 조건을 전부 만족하는지 검사해 개수를 센다.

각 조건은 `X~Yop K` 형식이며, 두 사람의 위치 차이에서 1을 뺀 값이 "두 사람 사이에 있는 인원 수(간격)"가 된다. 이 간격을 연산자(`=`, `<`, `>`)에 따라 K와 비교하면 조건 만족 여부를 판단할 수 있다.

## 풀이

```java
static final char[] NAMES = {'A', 'C', 'F', 'J', 'M', 'N', 'R', 'T'};

String[] data;
boolean[] used;
int[] pos;
int cnt;

public int solution(int n, String[] data) {
    this.data = data;
    used = new boolean[8];
    pos = new int[8];
    cnt = 0;
    dfs(0);
    return cnt;
}

public void dfs(int depth) {
    if (depth == 8) {
        if (isValid()) cnt++;
        return;
    }
    for (int i = 0; i < 8; i++) {
        if (used[i]) continue;
        used[i] = true;
        pos[i] = depth;
        dfs(depth + 1);
        used[i] = false;
    }
}

public boolean isValid() {
    for (String d : data) {
        if (!check(d)) return false;
    }
    return true;
}

public boolean check(String d) {
    int i = idx(d.charAt(0));
    int j = idx(d.charAt(2));
    char op = d.charAt(3);
    int k = d.charAt(4) - '0';
    int gap = Math.abs(pos[i] - pos[j]) - 1;
    if (op == '=') return gap == k;
    if (op == '>') return gap > k;
    return gap < k;
}

public int idx(char c) {
    for (int i = 0; i < 8; i++) {
        if (NAMES[i] == c) return i;
    }
    return -1;
}
```
