---
title: "[프로그래머스 150366] 표 병합"
date: 2026-09-13
last_modified_at: 2026-09-13
categories: [algorithm, implementation]
tags: [programmers, java, implementation]
toc: true
comments: true
---

## 문제

> 링크: [프로그래머스 150366](https://school.programmers.co.kr/learn/courses/30/lessons/150366)

50×50 크기의 표에서 `UPDATE`, `MERGE`, `UNMERGE`, `PRINT` 네 가지 명령어를 처리하는 표 편집기를 구현하는 문제.

## 접근

### Union-Find로 병합 그룹 관리

표 크기가 50×50 = 2,500칸, 명령어 수도 최대 1,000개로 작다는 점이 핵심이다. 병합된 칸들을 Union-Find로 그룹화하고, 각 그룹의 값은 대표(루트) 칸에만 저장한다.

가장 까다로운 부분은 `UNMERGE`다. 일반적인 Union-Find는 합치는 연산만 지원하고 분리는 지원하지 않는다. 하지만 전체 칸 수가 2,500개로 작기 때문에, 분리할 때 그룹 전체를 스캔해서 초기화해도 시간 제한 안에 충분히 들어온다.

명령어별 처리 방식은 다음과 같다.

- `UPDATE r c value`: `find`로 루트를 찾아 그 값을 갱신한다.
- `UPDATE value1 value2`: 전체 칸을 스캔하며 값이 일치하는 칸을 교체한다. 병합되어 대표성을 잃은 칸의 값은 항상 `null`로 비워두므로, 루트인지 따로 검사하지 않아도 값 비교만으로 충분하다.
- `MERGE r1 c1 r2 c2`: 두 그룹을 합칠 때 `(r1, c1)` 쪽 그룹의 값을 우선한다.
- `UNMERGE r c`: 그룹 전체를 스캔해 멤버를 먼저 모은 뒤, 모든 멤버를 초기화하고 지정한 칸에만 값을 되돌려준다. 스캔 없이 한 번에 초기화하면 먼저 처리된 칸이 자기 자신을 가리키게 되면서 아직 처리되지 않은 다른 멤버의 `find` 결과가 틀어지므로, 멤버 목록을 먼저 확보하는 순서가 중요하다.
- `PRINT r c`: `find`로 찾은 루트의 값을 출력하고, 없으면 `EMPTY`를 출력한다.

## 풀이

```java
import java.util.ArrayList;
import java.util.List;

int size = 50;
int[] parent;
String[] value;

public String[] solution(String[] commands) {
    int n = size * size;
    parent = new int[n];
    value = new String[n];
    for (int i = 0; i < n; i++) {
        parent[i] = i;
    }
    List<String> prints = new ArrayList<>();
    for (String command : commands) {
        String[] tokens = command.split(" ");
        switch (tokens[0]) {
            case "UPDATE":
                if (tokens.length == 4) update(Integer.parseInt(tokens[1]), Integer.parseInt(tokens[2]), tokens[3]);
                else update(tokens[1], tokens[2]);
                break;
            case "MERGE":
                merge(Integer.parseInt(tokens[1]), Integer.parseInt(tokens[2]), Integer.parseInt(tokens[3]), Integer.parseInt(tokens[4]));
                break;
            case "UNMERGE":
                unmerge(Integer.parseInt(tokens[1]), Integer.parseInt(tokens[2]));
                break;
            case "PRINT":
                prints.add(print(Integer.parseInt(tokens[1]), Integer.parseInt(tokens[2])));
                break;
        }
    }
    return prints.toArray(new String[0]);
}

public void update(int r, int c, String newValue) {
    value[find(index(r, c))] = newValue;
}

public void update(String oldValue, String newValue) {
    for (int i = 0; i < size * size; i++) {
        if (oldValue.equals(value[i])) value[i] = newValue;
    }
}

public void merge(int r1, int c1, int r2, int c2) {
    union(index(r1, c1), index(r2, c2));
}

public void unmerge(int r, int c) {
    int target = index(r, c);
    int root = find(target);
    String savedValue = value[root];
    List<Integer> members = new ArrayList<>();
    for (int i = 0; i < size * size; i++) {
        if (find(i) == root) members.add(i);
    }
    for (int member : members) {
        parent[member] = member;
        value[member] = null;
    }
    value[target] = savedValue;
}

public String print(int r, int c) {
    String v = value[find(index(r, c))];
    if (v == null) return "EMPTY";
    return v;
}

public int index(int r, int c) {
    return (r - 1) * size + (c - 1);
}

public int find(int x) {
    if (parent[x] == x) return x;
    return parent[x] = find(parent[x]);
}

public void union(int a, int b) {
    int rootA = find(a);
    int rootB = find(b);
    if (rootA == rootB) return;
    String mergedValue = value[rootA] != null ? value[rootA] : value[rootB];
    parent[rootA] = rootB;
    value[rootB] = mergedValue;
    value[rootA] = null;
}
```
