---
title: "[프로그래머스 142085] 디펜스 게임"
date: 2026-06-09
last_modified_at: 2026-06-09
categories: [algorithm, greedy]
tags: [programmers, java, greedy]
toc: true
comments: true
---

## 문제

> 링크: [프로그래머스 142085](https://school.programmers.co.kr/learn/courses/30/lessons/142085)

n명의 병사와 무적권 k개로 enemy 배열의 라운드를 순서대로 막을 때, 최대 몇 라운드까지 막을 수 있는지 구한다.

## 접근

### 그리디 + 최소 힙

무적권은 적이 가장 많이 등장하는 라운드에 사용하는 것이 최적이다. 따라서 지금까지 진행한 라운드 중 enemy 상위 k개를 무적권 대상으로 유지하면 된다.

최소 힙으로 크기 k짜리 창을 관리한다. 새 라운드의 enemy[i]를 힙에 넣고, 힙 크기가 k를 초과하면 최솟값(가장 덜 아낄 수 있는 라운드)을 꺼내 실제 병사를 소모한다. 이를 위해 새 값이 힙의 최솟값보다 크면 교체하는 방식으로 처리한다.

## 풀이

```java
import java.util.PriorityQueue;

public int solution(int n, int k, int[] enemy) {
    PriorityQueue<Integer> pq = new PriorityQueue<>();
    for (int i = 0; i < enemy.length; i++) {
        if (k-- > 0) pq.add(enemy[i]);
        else if (enemy[i] <= pq.peek()) n -= enemy[i];
        else {
            n -= pq.poll();
            pq.add(enemy[i]);
        }
        if (n == 0) return i + 1;
        else if (n < 0) return i;
    }
    return enemy.length;
}
```