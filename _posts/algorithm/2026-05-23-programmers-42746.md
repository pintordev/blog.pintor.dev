---
title: "[프로그래머스 42746] 가장 큰 수"
date: 2026-05-23
last_modified_at: 2026-05-23
categories: [algorithm, sort]
tags: [programmers, java, sort]
toc: true
comments: true
---

## 문제

> 링크: [프로그래머스 42746](https://school.programmers.co.kr/learn/courses/30/lessons/42746)

0 또는 양의 정수 배열이 주어졌을 때, 정수를 이어 붙여 만들 수 있는 가장 큰 수를 문자열로 반환하는 문제.

## 접근

### 커스텀 정렬

두 수 a, b를 이어 붙일 때 `ab > ba`이면 a를 앞에 배치해야 더 큰 수가 된다.
이 비교 기준을 Comparator로 적용해 정렬하면, 전체를 이어 붙인 결과가 최대가 된다.

정렬 후 첫 원소가 `"0"`이라면 모든 원소가 0이므로 `"0"`을 반환한다.

## 풀이

```java
import java.util.Arrays;

public String solution(int[] numbers) {
    String[] ns = new String[numbers.length];
    for (int i = 0; i < numbers.length; i++) ns[i] = String.valueOf(numbers[i]);
    Arrays.sort(ns, (a, b) -> (b + a).compareTo(a + b));
    if (ns[0].equals("0")) return "0";
    return String.join("", ns);
}
```