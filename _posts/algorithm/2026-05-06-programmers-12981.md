---
title: "[프로그래머스 12981] 영어 끝말잇기"
date: 2026-05-06
last_modified_at: 2026-05-06
categories: [algorithm, string]
tags: [programmers, java, string]
toc: true
comments: true
---

## 문제

> 링크: [프로그래머스 12981](https://school.programmers.co.kr/learn/courses/30/lessons/12981)

n명이 순서대로 끝말잇기를 할 때, 앞 단어의 마지막 글자로 시작하지 않거나 이미 사용한 단어를 말하면 탈락한다. 탈락자의 [번호, 차례]를 반환하라.

## 접근

### HashSet으로 중복 체크 + 선형 순회

각 단어를 순서대로 순회하며 두 가지 조건을 검사한다.

1. 앞 단어의 마지막 글자 ≠ 현재 단어의 첫 글자
2. 이미 사용된 단어 (HashSet으로 O(1) 판별)

위반이 발생한 인덱스 `i`에서 탈락자 정보를 계산한다.
- 번호: `(i - 1) % n + 1`
- 차례: `(i - 1) / n + 1`

첫 단어는 앞 단어가 없으므로 센티넬 `' '`(소문자 제약 덕에 입력에 등장하지 않음)을 초기값으로 두어 비교를 스킵한다.

## 풀이

```java
import java.util.HashSet;
import java.util.Set;

public class Solution {

    public int[] solution(int n, String[] words) {
        Set<String> set = new HashSet<>();
        char c = ' ';
        int i = 0;
        for (String word : words) {
            i++;
            if (c != ' ' && c != word.charAt(0)) break;
            if (!set.add(word)) break;
            c = word.charAt(word.length() - 1);
        }
        if (set.size() == words.length) return new int[]{0, 0};
        return new int[]{(i - 1) % n + 1, (i - 1) / n + 1};
    }
}
```