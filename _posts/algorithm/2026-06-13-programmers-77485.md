---
title: "[프로그래머스 77485] 행렬 테두리 회전하기"
date: 2026-06-13
last_modified_at: 2026-06-13
categories: [algorithm, implementation]
tags: [programmers, java, implementation]
toc: true
comments: true
---

## 문제

> 링크: [프로그래머스 77485](https://school.programmers.co.kr/learn/courses/30/lessons/77485)

rows × columns 행렬에서 직사각형 범위의 테두리를 시계방향으로 회전할 때, 각 회전마다 이동한 값 중 최솟값을 반환한다.

## 접근

### 시뮬레이션

행렬을 직접 메모리에 유지하며 쿼리를 순서대로 시뮬레이션한다. 쿼리 범위가 겹칠 수 있고, 이전 회전 결과가 다음 회전에 영향을 주므로 인덱스 계산만으로는 해결할 수 없다.

회전 구현은 `prev` 변수 하나로 처리한다. 테두리 첫 번째 셀의 값을 `prev`에 저장한 뒤, 상→우→하→좌 순서로 순회하면서 각 셀에 `prev`를 쓰고, 기존 값을 `tmp`에 옮긴다. 쓴 값(`prev`)이 곧 이동한 값이므로 여기서 최솟값을 갱신한다.

## 풀이

```java
int[][] map;

public int[] solution(int rows, int columns, int[][] queries) {
    map = new int[rows + 1][columns + 1];
    int n = 1;
    for (int i = 1; i <= rows; i++) {
        for (int j = 1; j <= columns; j++) {
            map[i][j] = n++;
        }
    }
    int[] res = new int[queries.length];
    for (int i = 0; i < queries.length; i++) {
        res[i] = rotate(queries[i][0], queries[i][1], queries[i][2], queries[i][3]);
    }
    return res;
}

public int rotate(int x1, int y1, int x2, int y2) {
    int min = Integer.MAX_VALUE;
    int prev = map[x1][y1];
    for (int i = y1 + 1; i <= y2; i++) {
        int tmp = map[x1][i];
        min = Math.min(min, map[x1][i] = prev);
        prev = tmp;
    }
    for (int i = x1 + 1; i <= x2; i++) {
        int tmp = map[i][y2];
        min = Math.min(min, map[i][y2] = prev);
        prev = tmp;
    }
    for (int i = y2 - 1; i >= y1; i--) {
        int tmp = map[x2][i];
        min = Math.min(min, map[x2][i] = prev);
        prev = tmp;
    }
    for (int i = x2 - 1; i >= x1; i--) {
        int tmp = map[i][y1];
        min = Math.min(min, map[i][y1] = prev);
        prev = tmp;
    }
    return min;
}
```