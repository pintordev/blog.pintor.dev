---
title: "[프로그래머스 70129] 이진 변환 반복하기"
date: 2026-04-30
last_modified_at: 2026-04-30
categories: [algorithm, greedy]
tags: [programmers, java, greedy]
toc: true
comments: true
---

## 문제

> 링크: [프로그래머스 70129](https://school.programmers.co.kr/learn/courses/30/lessons/70129)

0과 1로 이루어진 문자열에서 0을 모두 제거한 뒤 남은 1의 개수를 2진수 문자열로 변환하는 과정을 `"1"`이 될 때까지 반복하며, 변환 횟수와 제거된 0의 총 개수를 반환한다.

## 접근

### 시뮬레이션

매 반복마다 1의 개수를 세고, `전체 길이 - 1의 개수`로 0의 개수를 구해 누적한다. 이후 `Integer.toBinaryString()`으로 다음 문자열을 생성하며 문자열이 `"1"`이 될 때까지 반복한다.

## 풀이

```java
package programmers.greedy.p70129;

import java.util.Arrays;

public class Solution {

    public static void main(String[] args) {
        Solution s = new Solution();

        String[] ss = {"110010101001", "01110", "1111111"};
        int[][] answers = {{3, 8}, {3, 3}, {4, 1}};

        for (int i = 0; i < ss.length; i++) {
            System.out.println(Arrays.equals(s.solution(ss[i]), answers[i]));
        }
    }

    public int[] solution(String s) {
        int iter = 0, zc = 0, len;
        while ((len = s.length()) > 1) {
            int oc = 0;
            for (int i = 0; i < len; i++) {
                if (s.charAt(i) == '1') oc++;
            }
            zc += len - oc;
            iter++;
            s = Integer.toBinaryString(oc);
        }
        return new int[]{iter, zc};
    }
}
```